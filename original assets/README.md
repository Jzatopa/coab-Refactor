# Original assets

This folder contains copies of every original PNG asset currently preserved by the repo: all 231 extracted image-pipeline frames plus the two low-resolution originals from `extracted-assets`. Enlarged `_8x` derivatives are excluded because they are not original-resolution source assets.

Image count: **233**. See `MANIFEST.json` for paths, sizes, and SHA-256 hashes.
